const Button = () => {
  return (
    <a
      id="filter-add"
      className="waves-effect waves-light btn modal-trigger"
      data-target="modal1"
    >
      NOVO LANÇAMENTO
    </a>
  );
};

export default Button;
