{
  "engines": {
    "node": "^14"
  },
  "scripts": {
    "build": "cd ./backend && yarn && cd ../",
    "install:all": "yarn && cd ./backend yarn",
    "start": "cd ./backend && yarn start:prod"
  },
  "devDependencies": {
    "@types/cors": "^2.8.8",
    "git-commit-msg-linter": "^3.0.0"
  }
}
